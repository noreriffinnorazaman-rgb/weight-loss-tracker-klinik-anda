// API routes are no longer needed - using Apps Script directly from client
// This file is kept as a placeholder
import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ message: "Use Apps Script endpoint instead" });
}
